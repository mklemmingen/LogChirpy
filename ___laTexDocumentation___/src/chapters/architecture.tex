\chapter{System Architecture and Design}

\section{Architectural Overview and Design Principles}

LogChirpy uses a modular software architecture for cross-platform development. The system supports scalable bird data collection and processing with good performance on mobile devices.

LogChirpy builds on React Native with Expo SDK for cross-platform deployment across iOS and Android. The machine learning architecture combines \texttt{react-native-fast-tflite} for BirdNET audio classification with MLKit packages for object detection and image classification, processing entirely through local \texttt{.tflite} models without cloud dependencies. Data management uses SQLite for the local species encyclopedia and observation storage, with optional Firebase Firestore synchronization. Firebase Authentication provides user management with offline fallback capabilities, while OpenStreetMap integration handles geolocation visualization for bird sightings.

\textbf{Three-Pillar System Architecture:}

\begin{figure}[H]
    \centering
    \begin{minipage}{0.9\textwidth}
        \small
        \begin{verbatim}
+---------------------------------------------------------------------+
|                    LOGCHIRPY ARCHITECTURE                           |
+---------------------------------------------------------------------+
|                                                                     |
|  ML PROCESSING PILLAR        BIRDEX ENCYCLOPEDIA PILLAR             |
|  +-- Unified Pipeline        +-- 33,000+ Species Database           |
|  +-- Audio Classification    +-- 5-Language Translations            |
|  +-- Image Detection         +-- Wikimedia Image Integration        |
|  +-- Sequential Processing   +-- Search/Filter/Quiz Systems         |
|                                                                     |
|                    LOGGING SYSTEM PILLAR                            |
|                    +-- Multi-Modal Capture                          |
|                    +-- Draft Management                             |
|                    +-- Gallery Integration                          |
|                    +-- Cloud Synchronization                        |
|                                                                     |
+---------------------------------------------------------------------+
|  SUPPORTING INFRASTRUCTURE                                          |
|  +-- Authentication (Firebase)  +-- App Navigation Structure        |
|  +-- Context Providers          +-- Themed Component System         |
|  +-- Custom Hooks               +-- Configuration Management        |
+---------------------------------------------------------------------+
        \end{verbatim}
    \end{minipage}
    \caption{LogChirpy's Three-Pillar Architecture}
    \label{fig:system_architecture}
\end{figure}

The architecture follows modular design principles with clear separation between authentication, observations, ML processing, and media management. The offline-first approach prioritizes local data persistence while offering optional cloud synchronization. UI components maintain clean boundaries from business logic and state management, while internationalization support spans six languages through integrated i18n frameworks.

\section{Application Structure and Navigation Architecture}

\textbf{App Directory Organization:}

\begin{lstlisting}[style=javascript, caption=Application Structure, label=lst:folder_structure]
/app/(tabs)/              # Main tab navigation (6 tabs)
  ├── index.tsx           # Home screen with feature cards
  ├── birdex/            # BirDex encyclopedia interface
  ├── archive/           # Bird spotting archive and map
  ├── gallery.tsx        # Media gallery and export
  ├── account/           # User authentication
  └── settings.tsx       # App configuration

/app/log/                # Bird logging workflow
  ├── select.tsx         # Logging method selection
  ├── manual.tsx         # Manual text entry
  ├── photo.tsx          # Photo capture and classification
  ├── audio.tsx          # Audio recording
  ├── video.tsx          # Video capture
  └── objectIdentCamera.tsx  # AI-powered real-time detection

/components/             # Reusable UI components
  ├── ThemedView.tsx     # Base container with theme variants
  ├── ThemedText.tsx     # Typography with semantic colors
  ├── ThemedPressable.tsx # Interactive elements
  ├── BackButton.tsx     # Navigation component
  └── audio/             # Audio-specific components

/contexts/               # React Context providers
  ├── AuthContext.tsx    # Firebase authentication
  └── LogDraftContext.tsx # Persistent draft storage

/services/               # Business logic services
  ├── unifiedMLPipelineService.ts  # ML orchestration
  ├── ultraSimpleBirdClassifier.ts # Audio classification
  ├── database.ts        # Bird observations database
  ├── databaseBirDex.ts  # Species encyclopedia
  ├── sync_layer.ts      # Firebase synchronization
  └── uriUtils.ts        # File manipulation utilities

/hooks/                  # Custom React hooks
  ├── useThemeColor.ts   # Theme management
  └── useBirdDexDatabase.ts # Species database state
\end{lstlisting}

\textbf{Navigation Architecture:}
The application uses Expo Router with tab-based primary navigation organizing six main sections. The logging workflow is separated into dedicated screens supporting different input methods, from manual entry to automated ML processing.

\section{BirDex Encyclopedia Architecture}

\textbf{Comprehensive Species Database System:}

The BirDex encyclopedia represents one of LogChirpy's major architectural achievements, transforming a massive CSV dataset into a fully functional multilingual bird encyclopedia. The system manages over 33,000 global bird species with comprehensive metadata and localized content through \texttt{services/databaseBirDex.ts}.

\textbf{BirDex Database Structure:}

\begin{lstlisting}[style=javascript, caption=BirDex Database Schema, label=lst:birdex_schema]
-- BirDex comprehensive species database (databaseBirDex.ts)
-- 33,000+ global bird species with multilingual support
CREATE TABLE birddex (
    species_code           TEXT    PRIMARY KEY,
    english_name           TEXT    NOT NULL,
    scientific_name        TEXT    NOT NULL,
    category               TEXT,
    family                 TEXT,
    order_                 TEXT,
    range                  TEXT,
    de_name                TEXT,    -- German names
    es_name                TEXT,    -- Spanish names
    fr_name                TEXT,    -- French names
    ukrainian_name         TEXT,    -- Ukrainian names
    ar_name                TEXT,    -- Arabic names
    hasBeenLogged          INTEGER DEFAULT 0
);

-- Performance optimization indexes
CREATE INDEX idx_birddex_english_name ON birddex(english_name);
CREATE INDEX idx_birddex_scientific_name ON birddex(scientific_name);
CREATE INDEX idx_birddex_family ON birddex(family);
\end{lstlisting}

\textbf{Encyclopedia Features:}
\begin{itemize}
    \item \textbf{Multi-language search:} Name lookup across all supported languages
    \item \textbf{Taxonomic filtering:} Search by family, order, geographic range
    \item \textbf{Educational quizzes:} Interactive species identification testing
    \item \textbf{Visual integration:} Each species paired with locally stored Wikimedia images
    \item \textbf{Performance optimization:} Strategic indexing and pagination for 33,000+ records
\end{itemize}

\textbf{Multi-Language Data Enrichment:}
The encyclopedia system processes raw taxonomic data through comprehensive translation workflows, adding German, Spanish, French, Ukrainian, and Arabic common names to each species entry. This creates a truly multilingual reference system accessible across diverse user communities.

\section{Logging System Architecture}

\textbf{Multi-Modal Observation Management:}

The logging system serves as LogChirpy's core functionality, integrating all other architectural components into a cohesive observation workflow. The system handles three distinct media types (images, audio, video) combined with ML predictions, manual entries, GPS coordinates, and taxonomic classifications.

\textbf{Logging Workflow Architecture:}

\textbf{Observation Data Structure:}

\begin{lstlisting}[style=javascript, caption=Bird Observation Schema, label=lst:observation_schema]
-- Primary table for bird spotting records (database.ts)
CREATE TABLE bird_spottings (
    id               INTEGER PRIMARY KEY AUTOINCREMENT,
    image_uri        TEXT,
    video_uri        TEXT,
    audio_uri        TEXT,
    text_note        TEXT,
    gps_lat          REAL,
    gps_lng          REAL,
    date             TEXT,
    bird_type        TEXT,
    image_prediction TEXT,    -- ML classification results
    audio_prediction TEXT,    -- BirdNET audio results
    synced           INTEGER DEFAULT 0,
    latinBirDex      TEXT     -- Link to BirDex species
);

-- Performance indexes
CREATE INDEX idx_bird_spottings_date ON bird_spottings(date);
CREATE INDEX idx_bird_spottings_synced ON bird_spottings(synced);
\end{lstlisting}

\textbf{Draft Management System:}
The \texttt{contexts/LogDraftContext.tsx} provides persistent draft storage using AsyncStorage, enabling auto-save functionality and recovery of incomplete observations during field work and app interruptions.

\textbf{Media Integration:}
The logging system integrates with device storage through \texttt{services/photoStorageService.ts} and the gallery interface. Captured media is available for review, organization, and export through \texttt{app/(tabs)/gallery.tsx}.

\section{ML Processing Architecture}

\textbf{Unified ML Pipeline:}

The \texttt{services/unifiedMLPipelineService.ts} manages sequential ML processing to eliminate race conditions between image and audio operations, solving critical stability issues from concurrent ML processes.

\begin{figure}[H]
    \centering
    \begin{minipage}{0.9\textwidth}
        \footnotesize
        \begin{verbatim}
START
  ↓
┌─────────────────────────────────────┐
│         IMAGE PROCESSING PHASE      │
├─────────────────────────────────────┤
│ 1. Photo Capture (0.3 quality)      │
│ 2. Object Detection (MLKit)         │
│ 3. For each detected object:        │
│    a. Crop detection region         │
│    b. Classify with bird model      │
│ 4. Update UI (SVG overlays)         │
│ 5. Save high-confidence detections  │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│        AUDIO PROCESSING PHASE       │
├─────────────────────────────────────┤
│ 1. Audio Recording (3 seconds)      │
│ 2. Process with local whoBird       │
│ BirdNET fast-tflite models          │
│ in dual-pipeline of audio and       │
│ location/time                       │
│ 3. Update UI (predictions)          │
│ 4. Delete audio file (cleanup)      │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│            WAIT PHASE               │
├─────────────────────────────────────┤
│ Wait for configured delay           │
│ (Config.camera.pipelineDelay)       │
└─────────────────────────────────────┘
  ↓
REPEAT
        \end{verbatim}
    \end{minipage}
    \caption{Unified ML Pipeline Processing Flow}
    \label{fig:ml_pipeline}
\end{figure}

\textbf{Audio Classification System:}

The \texttt{services/ultraSimpleBirdClassifier.ts} implements BirdNET model integration with key processing methods: \texttt{processAudio()} at line 288 transforms audio files to model-ready format, \texttt{processAudioChunk()} at line 403 handles streaming audio analysis, and \texttt{classifyAudio()} serves as main classification entry point with dual-model processing.
The \texttt{services/ultraSimpleBirdClassifier.ts} implements BirdNET dual-model architecture adapted from the whoBIRD project. The system uses \texttt{react-native-fast-tflite} for stable mobile integration, processing 3-second audio clips at 48kHz with location-based enhancement when GPS data is available.

\textbf{Image Detection Integration:}
Visual processing uses MLKit packages from Infinite Red for object detection and image classification. The system captures photos at reduced quality (0.3) for ML processing, detects objects, crops detection regions, and classifies each cropped section for bird identification.

\section{State Management and Context Architecture}

\textbf{React Context Providers:}

React Context provides application-wide state management with clean separation between authentication, logging workflows, and UI state.

\textbf{AuthContext Implementation (\texttt{contexts/AuthContext.tsx}):} Handles Firebase Authentication for user sign-in, registration, and profile management, provides offline fallback for application functionality without mandatory authentication, and manages persistent authentication state across app sessions.

\textbf{LogDraftContext Implementation (\texttt{contexts/LogDraftContext.tsx}):} Provides AsyncStorage integration for automatic draft saving, timeout-protected auto-save functionality to prevent data loss, handles multi-modal observation data structures, and implements recovery system to restore incomplete observations after app interruption.

\textbf{Custom Hooks Architecture:}

\textbf{Theme Management (\texttt{hooks/useThemeColor.ts}):} Enables light/dark mode switching with consistent color systems, seamless theme integration across all UI components, semantic text styles and hierarchies, and consistent motion design across interactions.

\textbf{Database State Management (\texttt{hooks/useBirdDexDatabase.ts}):} Manages filtering and pagination for 33,000+ species records, provides efficient query handling with caching, and implements state management for cross-language searches.

\section{Cloud Architecture, User Interface, and System Integration}

\textbf{Firebase Cloud Synchronization Architecture (\texttt{services/sync\_layer.ts}):}

The system uses offline-first cloud synchronization through Firebase. Three Firestore collections handle \texttt{users} (profiles and settings), \texttt{observations} (bird data and metadata), and \texttt{media} (Firebase Storage references). Local SQLite serves as primary storage with optional cloud backup and user-controlled synchronization.

\textbf{Themed Component System and UI Architecture (\texttt{components/}):}

The design system ensures consistent UX through themed components: \texttt{ThemedView.tsx} (base containers), \texttt{ThemedText.tsx} (typography with semantic colors), \texttt{ThemedPressable.tsx} (interactive elements with haptic feedback), \texttt{BackButton.tsx} (navigation), and specialized audio components. The ML interface in \texttt{app/log/objectIdentCamera.tsx} features a cyberpunk-themed HUD with SVG overlays, confidence-based color coding, and integrated prediction displays.

\textbf{System Integration and Data Flow Architecture:}

The three-pillar architecture coordinates ML processing, encyclopedia data, and logging workflows. BirDex provides taxonomic foundation for ML classification. Logging integrates real-time ML results with observation capture. Encyclopedia-logging linkage connects entries through Latin names for taxonomic accuracy. The data flow combines ML predictions with manual entries, links BirDex taxonomic data, manages media through gallery integration, and synchronizes with Firebase cloud storage.

\section{Data Processing Infrastructure}

\textbf{Wikimedia Image Extraction System}

The system creates a comprehensive image dataset for 33,000+ bird species through automated Wikimedia Commons scraping using Python scripts for bulk image processing.

\textbf{Wikimedia Scraping Architecture (\texttt{dev/scripts/}):}

The image extraction system implements a robust pipeline including Wikipedia API integration for automated species page discovery and image identification, direct image downloading from Wikimedia Commons repositories, rate limiting and quota management with respectful API usage and exponential backoff, automated selection of high-quality relevant images, WebP conversion for mobile-optimized performance, batch processing for efficient handling of 33,000+ species with resume functionality, metadata extraction for image attribution and licensing preservation, and direct integration with app asset structure.

\textbf{Image Processing Workflow:}
\begin{enumerate}
    \item \textbf{Species Page Discovery:} Query Wikipedia for each bird species scientific name
    \item \textbf{Image Identification:} Extract primary species images from infoboxes and galleries
    \item \textbf{Quality Assessment:} Filter images based on resolution, relevance, and licensing
    \item \textbf{Batch Download:} Download images with proper attribution and error handling
    \item \textbf{Format Optimization:} Convert to WebP format and optimize for mobile devices
    \item \textbf{Integration:} Generate manifest files for app integration and caching
\end{enumerate}

\textbf{OpenAI-Powered Translation System}

The multilingual encyclopedia translates 33,000+ bird species names across five languages (German, Spanish, French, Ukrainian, Arabic) using OpenAI's API with fallback strategies.

\textbf{Translation Architecture (\texttt{dev/scripts/\_birdyDex\_massTranslationScripts/}):}

\begin{lstlisting}[style=javascript, caption=Translation System Implementation, label=lst:translation_system]
// Core translation workflow with OpenAI integration
const translateSpeciesBatch = async (speciesBatch) => {
  // 1. Public API fallback attempts
  for (const publicAPI of publicBirdAPIs) {
    try {
      const publicResults = await tryPublicAPI(speciesBatch, publicAPI);
      if (publicResults.coverage > 0.8) {
        return publicResults;
      }
    } catch (error) {
      console.warn(`Public API ${publicAPI.name} failed, trying next`);
    }
  }
  
  // 2. OpenAI fallback for comprehensive coverage
  const openAIResults = await translateWithOpenAI(speciesBatch, {
    model: 'gpt-4',
    maxTokens: 2000,
    temperature: 0.1, // Low temperature for consistent translations
    retryAttempts: 3,
    rateLimitBackoff: true
  });
  
  return openAIResults;
};
\end{lstlisting}

\textbf{Translation System Features:} Implements hybrid API strategy using public bird APIs first with OpenAI as comprehensive fallback, cost optimization through public API prioritization, quality assurance via low-temperature GPT-4 settings for consistent translations, efficient API usage through intelligent batching, automatic recovery from interruptions with progress tracking, persistent caching to avoid re-translating completed entries, exponential backoff and quota monitoring for rate limit management, automatic fallback between GPT-4 and GPT-3.5 based on availability, and real-time progress tracking with periodic CSV exports.

\textbf{Data Processing Pipeline:} Loads Clements v2024 taxonomy with 33,000+ species, queries public APIs for English common names, attempts translation through free ornithological APIs, fills gaps and enhances quality through GPT-4 translations, validates translations and handles special cases, produces final \texttt{birds\_fully\_translated.csv}, and imports enriched data into the app's SQLite database.

\textbf{Translation Quality Management:}
The system implements comprehensive quality controls including validation of translation completeness, detection of untranslated scientific names, handling of extinct species and regional variants, and maintenance of taxonomic accuracy across all target languages.

\section{Performance Optimization and Scalability}

\textbf{Database Performance Management:} Uses optimized indexes for species search across 33,000+ records, efficient handling of large datasets with 20-item pagination, 5-minute TTL cache for frequently accessed species data, and multi-stage search strategy from exact match to fuzzy matching.

\textbf{ML Pipeline Optimizations:} Unified sequential processing eliminates race conditions, 0.3 quality factor optimizes ML processing performance, top 2 results display prevents UI clutter, and configurable adaptive delays accommodate different device capabilities.

\textbf{Media and Storage Management:} Converts all bird images to WebP for mobile efficiency, systematically removes temporary ML processing files, implements on-demand model and image loading to minimize memory usage, and applies automatic media compression for storage efficiency.